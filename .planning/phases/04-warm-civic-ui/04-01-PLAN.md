---
phase: 04-warm-civic-ui
plan: 01
type: execute
wave: 4
depends_on: ["03-01"]
files_modified: [src/app/globals.css, src/app/page.tsx, src/app/layout.tsx]
autonomous: false
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Landing page has a clear 'Start' CTA"
    - "Global styles use the Warm Civic palette (Zinc-50/900)"
    - "Typography is consistent and legible"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page UI"
    - path: "src/app/globals.css"
      provides: "Global theme variables"
---

<objective>
Implement the 'Warm Civic' landing page and global design refinements.
</objective>

<tasks>

<task type="auto">
  <name>Apply Global Styles and Typography</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
    1. Update `src/app/globals.css`:
       - Set `background: theme(colors.zinc.50)` and `color: theme(colors.zinc.900)`.
       - Add a subtle paper-like texture or just a clean solid Zinc-50.
    2. Update `src/app/layout.tsx`:
       - Add Google Fonts import for `Public Sans` (or use Next.js font optimization).
       - Set basic metadata (Title: VoteWise, Description: Find your alignment).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Site background is Zinc-50.
    - Default text is Zinc-900.
    - Font is Public Sans.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement Landing Page</name>
  <files>src/app/page.tsx</files>
  <action>
    Rewrite `src/app/page.tsx` to be a clean hero section:
    - Large, centered headline: "Your Voice, Quantified."
    - Subtitle: "Find which candidates actually align with your stances, without the noise."
    - Primary Button: "Start Survey" (links to `/survey`).
    - Footer: "100% Client-side. Your data stays on your device."
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Page is centered and responsive.
    - Button routes to `/survey`.
    - Content matches the minimalist requirement.
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- The application feels cohesive and visually "Warm Civic".
- The entry point is clear and professional.
</success_criteria>
