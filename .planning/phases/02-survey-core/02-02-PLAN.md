---
phase: 02-survey-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/components/QuestionCard.tsx, src/components/SurveyWizard.tsx, src/app/survey/page.tsx]
autonomous: false
requirements: [SURV-01, SURV-02]

must_haves:
  truths:
    - "User can navigate between issues using Next/Previous"
    - "User can adjust a 0-10 slider for each issue"
    - "User can select a weight (Low/Med/High) for each issue"
    - "A progress indicator shows current position in the survey"
  artifacts:
    - path: "src/components/QuestionCard.tsx"
      provides: "Issue input UI (slider + weights)"
    - path: "src/components/SurveyWizard.tsx"
      provides: "Step-by-step navigation logic"
    - path: "src/app/survey/page.tsx"
      provides: "Survey entry point"
  key_links:
    - from: "src/components/QuestionCard.tsx"
      to: "src/store/useSurveyStore.ts"
      via: "store hooks"
---

<objective>
Implement the interactive survey wizard UI.
Purpose: Provide a focused, distraction-free interface for users to input their political stances and importance weights.
Output: QuestionCard component, SurveyWizard component, and a dedicated survey page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/store/useSurveyStore.ts
@src/lib/registry.ts
</context>

<tasks>

<task type="auto">
  <name>Create QuestionCard Component</name>
  <read_first>src/types/schema.ts, src/types/survey.ts, src/store/useSurveyStore.ts</read_first>
  <files>src/components/QuestionCard.tsx</files>
  <action>
    Create a functional component `QuestionCard` that takes an `Issue` and `UserResponse` as props.
    - Implement a Slider input for value (0-10). Use a standard HTML range input with custom Tailwind styles. (per D-02)
    - Implement a 3-tier weight selector (Low, Medium, High). Use simple buttons or a radio-like toggle. (per D-03)
    - Ensure it updates the `useSurveyStore` on change.
    - Styles: Use Zinc-based colors for a clean look (zinc-900 text, zinc-500 secondary).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/QuestionCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component correctly displays issue name and description.
    - Slider allows selecting 0 to 10.
    - Weight buttons correctly highlight the active selection.
    - Changes are reflected in the Zustand store.
  </acceptance_criteria>
  <done>QuestionCard component implemented and wired to store.</done>
</task>

<task type="auto">
  <name>Create SurveyWizard and Survey Page</name>
  <read_first>src/lib/registry.ts, src/store/useSurveyStore.ts</read_first>
  <files>src/components/SurveyWizard.tsx, src/app/survey/page.tsx</files>
  <action>
    1. Create `SurveyWizard` component:
       - Fetch issues for the current race from `registry.ts`.
       - Manage `currentStep` index in local state.
       - Render `QuestionCard` for the current issue.
       - Add "Previous" and "Next" navigation buttons. (per D-01)
       - "Next" button should change to "View Results" on the last step (functionality for results page can be a placeholder for now).
       - Add a Progress Bar or "Step X of Y" indicator at the top.
    2. Create `src/app/survey/page.tsx`:
       - Use `useSurveyStore` to initialize with a race ID (e.g., 'pres-2024').
       - Render `SurveyWizard`.
       - Wrap in a container that centers the content (max-w-xl).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/SurveyWizard.tsx src/app/survey/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Survey page loads and displays the first question.
    - Navigation allows moving through all issues in the race.
    - Progress bar updates as steps change.
    - Reaching the end shows a "View Results" CTA.
  </acceptance_criteria>
  <done>SurveyWizard and page implemented with navigation logic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Functional Survey Core (State + UI)</what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to `/survey`.
    2. Interact with the slider and weight selectors.
    3. Click "Next" to move through questions.
    4. Refresh the page and verify your answers and current progress are still there (localStorage check).
    5. Reach the end and verify the "View Results" button appears.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → UI | All interaction is client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | Survey Page | accept | Static client-side page, low risk of resource exhaustion. |
</threat_model>

<verification>
Ensure the survey flow is complete, persistent, and functional according to the wizard decisions.
</verification>

<success_criteria>
- User can complete the survey for a race.
- Local persistence is confirmed via page refresh.
- No console errors during navigation or state updates.
</success_criteria>

<output>
After completion, create `.planning/phases/02-survey-core/02-02-SUMMARY.md`
</output>
