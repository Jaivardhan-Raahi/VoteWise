---
phase: 02-survey-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/schema.ts, src/data/issues.json, src/lib/registry.ts, src/types/survey.ts, src/store/useSurveyStore.ts]
autonomous: true
requirements: [SURV-03]

must_haves:
  truths:
    - "Survey state survives page refresh via localStorage"
    - "Registry provides issue metadata (name, description) for survey display"
  artifacts:
    - path: "src/store/useSurveyStore.ts"
      provides: "Zustand store with persistence"
    - path: "src/data/issues.json"
      provides: "Issue metadata definitions"
    - path: "src/types/survey.ts"
      provides: "Survey state interfaces"
  key_links:
    - from: "src/store/useSurveyStore.ts"
      to: "localStorage"
      via: "persist middleware"
---

<objective>
Establish the data infrastructure and state management for the survey core.
Purpose: Ensure user stances and weights are captured and persisted securely on the client.
Output: Zustand store, issue metadata, and registry extensions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/types/schema.ts
@src/lib/registry.ts
</context>

<tasks>

<task type="auto">
  <name>Define Issue Metadata and Update Registry</name>
  <read_first>src/types/schema.ts, src/lib/registry.ts, src/data/candidates.json</read_first>
  <files>src/types/schema.ts, src/data/issues.json, src/lib/registry.ts</files>
  <action>
    1. Update `src/types/schema.ts`: Add `IssueSchema` with fields: `id` (string), `name` (string), `description` (string). Export `Issue` type.
    2. Create `src/data/issues.json` with entries for "climate", "economy", and "education" matching the IDs in candidates.json.
    3. Update `src/lib/registry.ts`:
       - Add `getIssues(): Issue[]` to return all issues.
       - Add `getIssuesByRace(raceId: string): Issue[]` that returns unique issues mentioned by candidates in that race.
  </action>
  <verify>
    <automated>npx ts-node -e "import { getIssues } from './src/lib/registry'; console.log(getIssues().length > 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/issues.json` contains metadata for all issues present in candidate stances.
    - `getIssues()` returns a validated array of Issue objects.
    - `getIssuesByRace('pres-2024')` returns 3 issues (climate, economy, education).
  </acceptance_criteria>
  <done>Registry updated with issue metadata support.</done>
</task>

<task type="auto">
  <name>Implement Persistent Survey Store</name>
  <read_first>src/types/schema.ts</read_first>
  <files>src/types/survey.ts, src/store/useSurveyStore.ts</files>
  <action>
    1. Create `src/types/survey.ts`:
       - `UserResponse` interface: `{ value: number, weight: number }`.
       - `SurveyState` interface: `{ responses: Record<string, UserResponse>, currentRaceId: string | null }`.
    2. Create `src/store/useSurveyStore.ts`:
       - Use `zustand` with `persist` middleware.
       - Name the store `vote-wise-survey-storage`.
       - Implement actions: `setAnswer(issueId: string, value: number)`, `setWeight(issueId: string, weight: number)`, `setRace(raceId: string)`, `resetSurvey()`.
       - Weights should map to numbers: Low=1, Med=2, High=3 (per D-03).
  </action>
  <verify>
    <automated>npx ts-node -e "import { useSurveyStore } from './src/store/useSurveyStore'; console.log(typeof useSurveyStore.getState().setAnswer === 'function')"</automated>
  </verify>
  <acceptance_criteria>
    - Zustand store is created and exports `useSurveyStore`.
    - `persist` middleware is configured for `localStorage`.
    - Store correctly manages a mapping of issueId to { value, weight }.
  </acceptance_criteria>
  <done>Zustand store implemented with persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → LocalStorage | User survey data is persisted locally. No PII is stored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | LocalStorage | accept | Only storing non-PII issue IDs and numeric values. Minimal privacy risk. |
| T-02-02 | Tampering | LocalStorage | accept | User can modify their own local state; no server-side impact. |
</threat_model>

<verification>
Verify that the registry returns issue metadata and the store can set/get persisted values.
</verification>

<success_criteria>
- Registry correctly fetches issues with name/description.
- Survey store persists across "reloads" (simulated in tests or manually checked in next plan).
</success_criteria>

<output>
After completion, create `.planning/phases/02-survey-core/02-01-SUMMARY.md`
</output>
