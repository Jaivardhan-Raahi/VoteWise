---
phase: "01-foundation-candidate-data"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["package.json", "src/types/schema.ts", "src/data/candidates.json", "src/data/races.json"]
autonomous: true
requirements: ["DATA-01", "DATA-02", "LOC-01"]
must_haves:
  truths:
    - "Project is initialized with Next.js 15 and Tailwind 4"
    - "Data schemas are defined with Zod"
    - "Seed data contains at least two candidates with sourced stances"
  artifacts:
    - path: "src/types/schema.ts"
      provides: "Zod schemas for candidates and races"
    - path: "src/data/candidates.json"
      provides: "Mock candidate data"
  key_links:
    - from: "src/data/*.json"
      to: "src/types/schema.ts"
      via: "Structural compliance"
---

<objective>
Initialize the project foundation and seed core data.
Purpose: Provides the skeletal structure and data for the demo.
Output: Initialized project, schemas, and seed JSON.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-candidate-data/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization</name>
  <files>package.json</files>
  <action>
    Initialize Next.js 15 project in current directory.
    `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --no-git`
    
    Install only absolutely necessary dependencies:
    `npm install zustand zod`
  </action>
  <acceptance_criteria>
    - package.json contains "next": "15.x", "zod", "zustand".
  </acceptance_criteria>
  <done>Next.js foundation ready.</done>
</task>

<task type="auto">
  <name>Task 2: Define Schemas and Seed Data</name>
  <files>src/types/schema.ts, src/data/candidates.json, src/data/races.json</files>
  <action>
    1. Create `src/types/schema.ts` with Zod schemas for Stance (0-10 value, sourceUrl), Candidate, and Race.
    2. Create `src/data/candidates.json` with 2 mock candidates (3 stances each, including per D-02 sourceUrls).
    3. Create `src/data/races.json` with 1 hardcoded race ("pres-2024").
  </action>
  <acceptance_criteria>
    - `schema.ts` enforces 0-10 scale and sourceUrl requirement.
    - JSON files exist with valid mock data.
  </acceptance_criteria>
  <done>Data foundation established.</done>
</task>

</tasks>

<success_criteria>
Project is initialized and seed data is available for the registry.
</success_criteria>
