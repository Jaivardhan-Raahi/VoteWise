---
phase: "01-foundation-candidate-data"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01-01"]
files_modified: ["src/lib/scoring.ts", "src/lib/registry.ts"]
autonomous: true
requirements: ["ALGN-01"]
must_haves:
  truths:
    - "Simple weighted sum scoring calculates alignment 0-100"
    - "Registry returns validated candidate data"
  artifacts:
    - path: "src/lib/scoring.ts"
      provides: "Weighted sum alignment logic"
    - path: "src/lib/registry.ts"
      provides: "Data access utility"
---

<objective>
Implement core logic and data access.
Purpose: Connects the raw data to the alignment math for the demo.
Output: Scoring and Registry utilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-candidate-data/01-01-PLAN.md
@.planning/phases/01-foundation-candidate-data/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scoring and Registry</name>
  <files>src/lib/scoring.ts, src/lib/registry.ts</files>
  <action>
    1. Implement `calculateAlignment` in `src/lib/scoring.ts`:
       Sum `(10 - abs(user - candidate)) * weight` and normalize to 0-100.
    2. Implement `getRaces` and `getCandidatesForRace` in `src/lib/registry.ts` using the JSON files.
    3. Use Zod schemas to validate data on import in the registry.
  </action>
  <acceptance_criteria>
    - Scoring logic uses 0-10 scale and weight (1-3).
    - Registry functions return typed, validated data.
  </acceptance_criteria>
  <done>Core logic and registry ready.</done>
</task>

</tasks>

<success_criteria>
Matching logic and data access are fully implemented for the demo.
</success_criteria>
