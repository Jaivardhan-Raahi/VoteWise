---
phase: 03-results-comparison
plan: 01
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [src/app/results/page.tsx, src/components/SurveyWizard.tsx]
autonomous: false
requirements: [ALGN-02]

must_haves:
  truths:
    - "Calculates alignment for all candidates in the race using existing logic"
    - "Only top 3 candidates are shown"
    - "Page displays alignment percentage clearly"
  artifacts:
    - path: "src/app/results/page.tsx"
      provides: "Results display page"
    - path: "src/components/ResultCard.tsx"
      provides: "Individual candidate result UI"
---

<objective>
Implement the survey results page showing the top 3 matching candidates.
</objective>

<tasks>

<task type="auto">
  <name>Implement Results Calculation Logic</name>
  <read_first>src/lib/scoring.ts, src/lib/registry.ts, src/store/useSurveyStore.ts</read_first>
  <files>src/app/results/page.tsx</files>
  <action>
    Create the `/results` page structure.
    - Fetch user responses and race ID from `useSurveyStore`.
    - Fetch candidates and issues for that race from `registry.ts`.
    - For each candidate, calculate alignment using `calculateAlignment`.
    - Sort candidates by score (descending).
    - Take the Top 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/results/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Candidates are correctly scored based on survey data.
    - Only top 3 candidates are extracted.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create ResultCard Component</name>
  <files>src/components/ResultCard.tsx</files>
  <action>
    Create a simple `ResultCard` component:
    - Props: `Candidate`, `alignmentScore` (number).
    - Display: Candidate Name, Party (badge style), and Score (large text).
    - Visual: Use Zinc-900 for high scores, Zinc-500 for labels.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/ResultCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component renders candidate info correctly.
    - Alignment score is displayed as a percentage.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Complete Results Page UI & Wiring</name>
  <files>src/app/results/page.tsx, src/components/SurveyWizard.tsx</files>
  <action>
    1. Finalize `/results` page:
       - Render the list of top 3 `ResultCard`s.
       - Add a "Retake Survey" button that calls `resetSurvey` and routes back to `/`.
    2. Update `SurveyWizard.tsx`:
       - Change the "View Results" logic to use Next.js `router.push('/results')`.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/results/page.tsx src/components/SurveyWizard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Navigation from survey to results works.
    - Results page shows the top 3 matches.
    - Retake button clears state and navigates back.
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- User can see their top 3 candidate matches after completing the survey.
- The scoring uses the logic established in Phase 1.
- No more than 3 candidates are shown.
</success_criteria>
